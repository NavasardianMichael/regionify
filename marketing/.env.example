# Origin of the Regionify web app (links from marketing go here). No trailing slash.
# Required. Match CLIENT_URL used by the server and client in each environment.
CLIENT_URL=

# Regionify account credentials for provisioning marketing showcase projects.
# Required to run `pnpm run marketing:assets`.
REGIONIFY_EMAIL=
REGIONIFY_PASSWORD=
