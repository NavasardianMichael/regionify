VITE_API_BASE_URL=http://localhost:9002

# Paddle (browser checkout overlay) — omit to disable client-side checkout
# Client-side token from Paddle dashboard → Developer tools → Authentication → Client-side tokens
VITE_PADDLE_CLIENT_TOKEN=
# "sandbox" or "production" (must match server PADDLE_SANDBOX)
VITE_PADDLE_ENV=sandbox
