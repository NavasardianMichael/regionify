**/node_modules
**/dist
**/*.log
.git
.gitignore
*.md
.vscode
.idea
.husky
coverage
.nyc_output

# Client source must be in the build context: server/Dockerfile builder runs
# `pnpm --filter @regionify/client build`. The runtime stage only copies
# `client/dist` from the builder image, not full client sources.

# Env files (passed separately)
**/.env*
!**/.env.example
